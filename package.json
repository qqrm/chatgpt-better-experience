{
  "name": "chatgpt-dictation-auto-send",
  "private": true,
  "version": "0.0.2",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "lint:amo": "npx web-ext@9.3.0 lint --source-dir dist",
    "build:amo": "npx web-ext@9.3.0 build --source-dir dist --artifacts-dir web-ext-artifacts --overwrite-dest",
    "verify:ci": "npm run verify && npm test && npm run build && npm run lint:amo && npm run build:amo",
    "generate:icons": "node scripts/generate-icons.mjs",
    "lint": "eslint .",
    "format:check": "prettier --check .",
    "format:write": "prettier --write .",
    "typecheck": "tsc --noEmit",
    "verify": "node scripts/verify.mjs",
    "prepare": "husky",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{js,mjs,cjs,json,md,html,css,scss,yml,yaml}": [
      "prettier --write"
    ]
  },
  "dependencies": {
    "@rrweb/record": "^2.0.0-alpha.18",
    "@rrweb/types": "^2.0.0-alpha.18"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@resvg/resvg-js": "^2.6.2",
    "@types/node": "^25.2.3",
    "@typescript-eslint/eslint-plugin": "^8.54.0",
    "@typescript-eslint/parser": "^8.56.0",
    "esbuild": "^0.27.3",
    "eslint": "9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.56.0",
    "vitest": "^4.0.18",
    "jsdom": "^28.1.0"
  }
}
